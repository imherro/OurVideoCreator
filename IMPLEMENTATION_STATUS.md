# 实现与验收记录

更新：2026-09-11。整体目标仍在开发，以下按实际证据区分能力与待验收范围。

## 已落地

- React Flow 画布、节点连线与编辑、分镜表、素材库、角色设定、模型服务设置。
- 可移除素材引用及关联连线；输入和结果版本变化沿下游传播过期状态，保留等待同批次结果的依赖。批量画布执行也会把静态参考素材写入下游任务快照，不会丢失图生视频首帧。
- 个人提示词模板库：编辑、历史版本、归档与跨页面冲突校验；内置模板可复制编辑。
- Web 同源服务、单用户密码与会话、自动保存、项目乐观锁和历史版本；保存冲突时阻止切换项目和提交生成。
- PostgreSQL 持久化队列、DAG 上游依赖、SSE 更新、取消保护、提交幂等与服务配置快照；P2 仍限制为一个独立 Worker 进程。
- 服务重启保留任务编号；Maestro、ComfyUI、视频网关恢复只查询原任务，不再次提交。缺失任务编号时不自动重试。
- llama-server 本地文本和结构化分镜；完成后空闲 60 秒卸载，媒体生成前释放文本模型。
- 模型目录接入选择器，按节点用途过滤服务、标记未安装模型；Maestro 帧率、合法帧数和参考图数量前置校验。批量提交事务化，任一节点校验失败整批回滚。
- 默认使用本项目 Qwen 27B、Flux Base 9B + LoRA、MiniMax H3 + Qwen3-VL Q2_K，通过本项目内置服务推理。ComfyUI 工作流、OpenAI 兼容图文、可配置视频网关可接入；MiniMax Hailuo 2.3 原生视频协议已接入，支持文生和单首帧图生、任务编号持久化及中断恢复查询。Replicate 原生 prediction 协议已接入，可用官方的 Seedance、Kling、Veo、Flux 等模型或版本化社区模型，并支持图像 data URI、任务恢复和远端取消。
- Twick 多轨剪辑工作区：项目素材适配、AI 分镜初剪、持久化恢复、拖动/trim/split/重叠、吸附、快捷键、视频/音频/图片/标题/字幕轨、素材替换、静态 transform、滤镜、转场、音量关键帧和音画淡入淡出。
- 新编辑工程由独立 render compiler 编译为 native FFmpeg filter graph，支持多轨合成、图片 overlay、相邻片段真实交叉淡化、标题/字幕、多路音频、音乐、音量自动化、transform 和滤镜；继续复用原队列、取消、恢复及资产登记。原 `Clip[]` 与 legacy 导出仍保留。
- Windows 安装与后台启动脚本。界面不依赖外部字体；局域网 HTTP 下可创建节点。

## 最新指定模型与独立推理迁移

用户明确改为项目内置推理。已导入独立源码、Python 3.10 环境及所需权重；7870 内置引擎独立启动通过。Qwen 27B 中文生成实测约 27.38 秒；Flux Base 9B + 指定 LoRA 完整生成与素材登记通过（512×512，约 112 秒）；MiniMax H3 + Qwen3-VL Q2_K 完整生成通过（864×480、24fps、5.17 秒、带音轨；引擎约 16 分 11 秒），素材登记与全片解码通过。验收样例保存在 data/validation。Q4 下载已按用户要求停止。

以下为迁移前的历史验证，不能替代最新指定组合的验收。

## 本机真实推理验证

环境：RTX 3060 12GB，现有 Maestro 环境与权重。各项目均在独立临时数据目录运行，未覆盖实际工作室账户或项目。

| 环节 | 实际结果 | 耗时 |
| --- | --- | --- |
| Gemma 本地文本 | 返回中文故事正文 | 约 4.3 秒 |
| Gemma 分镜 | 2 个镜头、合计 10 秒，结构化 JSON 校验通过 | 约 12.5 秒 |
| Flux 2 Klein 9B 生图 | 512×512 橘猫与纸箱机器人 JPEG，已查看图像 | 约 11.5 秒 |
| LTX 文生视频 | 512×512、25fps、1.96 秒，带音轨，可解码 | 约 134.3 秒 |
| LTX 图生视频 | 以上述图片为参考，512×512、25fps、1.96 秒，带音轨 | 约 58 秒 |

这些耗时仅对应本次参数、缓存与硬件状态，不是速度承诺；尚未通过一条浏览器操作完整验收创作闭环。

## 自动化与服务检查

- TypeScript/Vite 生产构建通过；93 项 Python 测试以及 35 项前端图依赖、默认节点、分镜、时间线和编辑器测试通过。Phase 3 / 4 的逐项证据见 `PHASE_3_4_ACCEPTANCE.md`。
- API 测试覆盖登录、跨站请求、密钥屏蔽、素材归属与 Range、保存冲突、幂等、取消竞态、DAG、重启与三个适配器的恢复协议。
- FFmpeg 实测 legacy 双镜顺序；另以 440Hz 原声和 880Hz 配乐检查混音、静音以及字幕烧录。新编辑器导出以真实媒体验证视频底轨、图片叠加位置、标题可见像素、双路音频、音量关键帧和相邻红蓝画面的交叉淡化。
- 主机 7868 健康接口正常，实际工作室密码已由用户设置。测试使用临时数据库。

## 仍需完成 / 验收

- 更多供应商能力参数和费用展示；除 MiniMax Hailuo 2.3 与 Replicate 平台外的厂商原生云视频接口尚未全面实现，两种云服务均还没有真实云账户验证。
- 更完整的跨服务 GPU 协调与分支重跑体验；同数据目录的队列进程锁已完成，进程退出自动释放。
- 更深入的中文分镜语义验收；字段和目标总时长校验、最多一次模型修复已实现并测试。
- 更完整的资产版本产品流程仍可继续优化；编辑时间线已按稳定 `assetId` 绑定素材，替换素材时保留剪辑参数，并在主机 URL 变化时重新绑定当前媒体接口。
- 3D 导演台已实现角色/方块布景、机位调整与保存、构图截图转图像节点，真实浏览器 WebGL 操作待验收；宫格分镜及分页 PNG 图板已实现。全景素材投影、视角构图保存与全景生图节点入口已实现，浏览器交互和生成接缝质量尚待验证。
- 分镜提示词修改同步到已有生成节点，时长按模型合法帧数更新，动作变更提示核对提示词。
- 浏览器交互与另一台电脑的完整实测，最终需求逐项验收。

内置推理已按用户要求移植 Maestro / WanGP 源码并保留许可，模型各自的许可证仍适用。云端适配器能力必须以具体协议和测试为准，不能表述为已经支持市场上所有模型。

首尾帧：视频节点支持独立尾帧选择；内置适配器使用 IE 控制并校验素材归属和模型能力。上传映射与能力拒绝测试通过，指定 MiniMax 首尾帧实际生成尚待验收。

任务进度：持久化开始/结束时间、引擎步数与 ETA；界面区分排队和运行，终态停止计时。数据库增量迁移及迟到事件保护已测试。


### 浏览器验收（2026-09-10）

用户明确授权后，在独立项目“浏览器验收 · 独立测试”完成：新建/命名项目；3D WebGL 场景显示、角色编辑、机位保存、构图生成参考图节点；页面刷新后角色与机位恢复；全景方向调整与 PNG 裁图回写；静态图加入时间线并播放；提交 5 秒 720P 导出任务，浏览器显示完成并提供 MP4。全景本轮使用构图图测试交互与保存，真实 2:1 全景画质另需验证。

发现内置引擎未启动时目录报连接失败，增加本项目引擎自动启动、启动中轮询提示和进程内启动互斥。37 项后端测试通过，前端生产构建通过（3D 延迟加载包有体积提示）。尚未完成全部云端供应商实测、另一台电脑实际操作以及完整长片全流程验收。


### 双页面冲突验收（2026-09-10）
在独立测试项目打开两个浏览器页面；A 保存概要后，B 使用旧 revision 保存触发冲突弹窗。B 备份草稿并加载 A 最新概要，再恢复 B 草稿保存。PostgreSQL 确认 B 内容持久化，历史版本保留 A 内容。自动保存遇到 409 后暂停，浏览器会话保留可恢复草稿并触发 JSON 下载备份。跨设备网络环境仍需另一台实际设备验收。


### 分镜与原生串联验收进展
Qwen3.8 27B 实测输出两镜各 5 秒、总计 10 秒，结构校验一次通过，耗时 177.7 秒。串联脚本使用其首镜提示词生成 Flux Base + LoRA 图像，耗时 178.79 秒；图像目视包含雨巷、橘猫、纸箱和机器人。MiniMax 任务已引用本次图像，仍待结束与解码验证。观察到首帧机器人已亮灯，而后镜要求灯从熄灭亮起，故增强默认分镜提示词：明确动作前首帧状态和相邻镜头状态衔接。该提示词改动尚未做新一轮模型质量验收，不追改当前运行任务。


### 原生单镜串联验收通过（2026-09-10）
实际 Qwen 分镜提示词 → Flux 2 Klein Base 9B + LoRA 图像 → MiniMax H3 + 现有 Q2_K 编码器首帧视频 → FFmpeg 导出全部完成。图片 864×480；视频 864×480、24fps、5.166667 秒、含音频；导出 1280×720、24fps、5.034667 秒、含音频。视频与导出均完整解码成功，数据库输入证明引用本次生成素材。验收材料保存 data/validation/pipeline-20260910。本次只覆盖一个镜头，不能替代多镜长片、尾帧控制、原生云端接口或另一电脑的验收。

### 原生双镜串联验收通过（2026-09-10）

隔离验收从已验证的两镜分镜输入出发，分别生成两张 864×480 Flux 图像及对应的两段 MiniMax H3 图生视频，再按分镜顺序导出。两段视频均为 864×480、24fps、5.166667 秒并带音轨；最终文件为 1280×720、24fps、10.034667 秒并带音轨。验证器检查了两段视频对各自首帧的引用、时间线顺序和所有媒体的完整解码。材料保存于 `data/validation/pipeline-multishot-20260910`。首帧画面目视满足雨巷橘猫与机器人、再到机器人胸灯和猫爪的构图连续性；此输入使用的是增强“动作前初始状态”提示词之前生成的旧分镜，第一镜机器人眼部已亮，不能作为新提示词状态控制质量的证明。

### 首帧状态与人工验收（2026-09-10）

分镜模板现在会将“灯熄灭、无发光、尚未接触”等动作前可见状态同时写进图像提示词。真实 Qwen 27B 输出已确认包含这些约束；Flux Base + LoRA 的一次实际生成仍将胸灯画为亮起，说明该模型对这类否定状态不能只依赖文本保证。画布因此增加“首帧状态核验”：状态敏感的分镜图每次生成或换版本后，需要在预览中人工确认，关联视频才可提交；服务端也会阻止批量任务绕过此检查。普通参考图和普通图生视频流程不受影响。

截至本次提交，56 项 Python 测试、16 项前端逻辑测试和 Vite 生产构建均通过。按当前需求，不再继续运行耗时的原生视频质量验收；后续以人工在浏览器中使用参考图生成视频为准。

### 交付前部署审计（2026-09-10）

当前工作室数据库已保存 `maestro-image`（Flux 2 Klein Base 9B + 指定 LoRA）和 `maestro-video`（MiniMax H3 + Qwen3-VL Q2_K）两个本地默认服务；项目内独立推理环境、Qwen 27B、Flux 权重和 LoRA、MiniMax H3/编码器/VAE 均通过文件清单核验。7868 Web 服务健康检查通过。新增契约测试确认：参考图片会上传到内置引擎，并作为 Flux 的 `KI` 参考图输入提交；MiniMax 使用单首帧输入。`npm test` 已成为前端逻辑测试的标准命令。

### 已确认的交互待改进

- **剧本到分镜的可视依赖线**：点击剧本节点结果中的“继续拆解分镜”时，当前会复制剧本文本创建分镜节点，但不会画出或保存“剧本 → 分镜规划”连线。后续应自动创建该连线，使画布关系与实际输入一致；用户仍可按需要删除连线或编辑分镜提示词。
