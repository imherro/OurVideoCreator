# UPSTREAM-SYNC-00：上游增量分类与实施顺序

> 2026-09-19：本文件保留首轮历史快照；当前逐项完成/待适配状态与新上游检查点见 [剩余清单](2026-09-19-REMAINING.md)，不能按下面初始“未开始”判断当前进度。

> 后续用户覆盖（2026-09-17）：不需要ChatGPT审核，由Codex独立完成；不能直接复制的先研究功能需求，在协作版按需适配；bug先在协作版复现，有则修复。以下清单和外审建议保留为最初规划记录，不再构成发送/等待外审的要求。当前实施状态见 [执行记录](PROGRESS.md)，不以复制全部SHA作为完成目标。
>
> `13939a8` 之后重新获取到的 23 个提交已单独记录在 [UPSTREAM-SYNC-37 增量清单](2026-09-17-INCREMENT-13939a8-564dd22.md)。本文件的 57 提交冻结统计不包含该新增范围。

日期：2026-09-17。状态：READY_FOR_REVIEW（清单，不是代码移植验收）。

用户在讨论选择性移植方案后明确要求“好，那依次完成吧”。本轮先完成一个可独立审核的清单任务；后续按单一任务实施、验证、提交和外部审核，不一次性合并57个提交。

## 1. 固定输入与检查范围

- 唯一开发/推送仓库：`imherro/OurVideoCreator`，起点 `master` / `4fba7892daeafff017eae43b7295ac0054b99391`，检查开始时工作区干净。
- 只读上游：`imherro/MyVideoCreator`；`git ls-remote upstream refs/heads/master` 实际返回 `13939a82fb23ea7659cc079e575c521c104789d8`。
- 上游本机工作副本 HEAD 与远端相同，工作区干净；本轮从该副本读取提交，不修改上游。
- 共同来源基线：`464914c553f4c1856ca77da4a07e9d5fffb7f71e`。协作仓库缓存的 `upstream/master` 仍停在此处，不能把它误认为远端最新。
- 冻结范围 `464914c..13939a8`：57提交，136文件，7668行增加/973行删除；与协作版自基线改动有52条路径重合。52是路径重合数，不是已经证明的文本冲突数。
- 本轮读取提交清单、文件统计、重点差异、上游功能说明与协作版对应实现。下表是风险分流和可见依赖初筛，**不是57提交全量逐行安全审计，也不是已经证明可直接cherry-pick**。每批实施前仍须读完整相关差异和最终文件，核对传递依赖。
- 未fetch、merge、cherry-pick或创建集成工作区；未运行产品测试、未调用供应商、未改业务、依赖、迁移、配置或后台页面；未操作任何数据库、媒体或服务。本轮仅新增本清单。

P0–P5、P6-SINGLE-01和FINAL-FUNC-01历史验收保持原SHA绑定，不自动继承为新移植代码的通过结论。运维、部署、多Worker、完整配额、自动恢复与真实付费API继续不在范围内。

## 2. 分类与依赖清单（上游时间顺序）

类别：**小修**＝候选独立缺陷修复；**适配**＝需要接入协作主源/权限/版本/任务契约；**界面**＝展示改进候选，仍须保留授权与草稿逻辑；**不原样导入**＝存在明确单机语义或已由协作机制替代。所有条目的实际移植状态均为“未开始”。依赖栏是已观察到的关系或核查点，不是批准整组导入。

| # | 上游SHA | 改动 | 分类 | 依赖/协作处理 |
| --- | --- | --- | --- | --- |
| 01 | ac2414d | 生成图片遵守项目画幅 | 适配 | 入队参数与平台允许范围核对；与56统一图像参数一起梳理 |
| 02 | dac84be | Provider及项目模型治理 | 不原样导入 | 上游providerId/modelId及local模型池与P4平台model_id契约不同；保留P4，按实际需求检查显示缺口 |
| 03 | a776c93 | 素材工作区与画布布局 | 界面 | 含managedGraph；布局投影不能偷偷写共享对象或越权改图 |
| 04 | 504e0c4 | 重组Provider和运行设置 | 不原样导入 | 单机设置/本地运行入口不适用；不能恢复普通成员Key和运行环境配置 |
| 05 | 83b8657 | 工作流规格、编辑器素材标签 | 适配 | common.register与editor素材字段；保持候选登记、素材归属及签名读取；07/49沿用该素材助手 |
| 06 | 24f4c52 | Seedance 2.0支持 | 适配 | 拆出适配器协议；不复制store启动迁移/自动改默认模型；冻结配置仍由P4管理 |
| 07 | edf241f | Twick多片段播放 | 适配 | 含postinstall补丁、素材时长与持久化；05及后续09/12组成编辑器检查链，保留租约 |
| 08 | 7276944 | 画布连线和连续预览 | 适配 | 可拆canvasEdges与TimelinePreview；图结构对象写入及播放状态边界需核验 |
| 09 | bba17a0 | 编辑器时长及切片 | 适配 | 接续07的Twick补丁；新增timelineDuration，与12/17一起核对最终时长语义 |
| 10 | a31eb62 | 样片导出流程 | 适配 | worker导出命名及前端设置；不能恢复文档整份保存或改变任务快照/租约 |
| 11 | cdf1dec | 视频编译结果过期判断 | 适配 | graph/video_dialogue；必须保持候选、绑定版本及旧任务晚到保护 |
| 12 | 032dc96 | 初剪播放与时长模式 | 适配 | 接续07/09；会改initialTimeline和Twick补丁，不能只挑最后一个修复 |
| 13 | 8e17006 | 放宽同项目浏览器冲突 | 不原样导入 | 409自动读远端并清dirty/草稿，直接违背协作显式比较及保留草稿契约 |
| 14 | 0cff9aa | 新项目云模型默认值 | 不原样导入 | 按厂商/模型前缀自动挑选；保留管理员显式默认，无默认不回落 |
| 15 | 0e8ce1c | 简化当前项目标题 | 界面 | main.tsx局部展示，不能删除协作身份/保存状态 |
| 16 | 83055bb | API错误响应只读一次 | 小修 | 3文件；保留CSRF头和错误status/url，建议独立第二批 |
| 17 | 7464df2 | 初剪保留完整素材 | 适配 | 接续12；后端投影与前端timeline单主源一致，不能绕过租约 |
| 18 | 62d4b09 | 原著章节与改编界面同步 | 适配 | 章节/策划关系主源与未保存草稿、切页同步需接现有协作机制 |
| 19 | 4dfb676 | 扩展长篇时保护已完成分集 | 适配 | adaptation/app/contracts/worker/store；只移业务保护意图，不复制自动应用结果路径 |
| 20 | 87c470a | 分集连续性与审核状态范围 | 适配 | 接续19；冻结上下文及各集审核revision，不能跨集自动改他人成果 |
| 21 | f96b53b | 作品级任务活动与剧本去重 | 适配 | 与P6-SINGLE幂等及P5候选规则核对；不能以非原子查询替代现有准入 |
| 22 | 67c9f5b | 动作参考和显式多模态 | 适配 | 新编译器/参考素材/静音派生文件/预览；接平台能力、对象保存、媒体ACL及任务冻结 |
| 23 | 10acb73 | 角色音色样本参考 | 适配 | 依赖22多模态清单；试听与确认版本分离，保留P5显式采纳 |
| 24 | eee2ebe | 跨集视觉历史和资产复用 | 适配 | 与P5分镜候选原子采纳、视觉版本及FINAL素材删除保护一起检查 |
| 25 | 045853a | 从历史恢复废弃视觉版本 | 适配 | 核对24的历史路径；授权恢复追加新版本，不原地覆盖锁定/审核版本 |
| 26 | 0866461 | 命名音色库和角色状态音色 | 适配 | 依赖23，新增voice_resolution；状态继承与负责人/版本边界需适配 |
| 27 | 117f843 | 区分动作参考上传与选素材 | 界面 | 依赖22的MotionReferenceEditor，不单独创建缺失组件 |
| 28 | c3cad55 | 参考素材分类与提交预览 | 适配 | 接22/23；涉及上传分类与共享素材过滤，不能只移显示层 |
| 29 | fd9a3da | 提交提示词内嵌参考缩略图 | 界面 | 接28，遵守认证媒体URL及权限失效规则 |
| 30 | 47e0409 | 白模动作参考文案 | 界面 | 依赖22组件 |
| 31 | b21aa66 | 预览排版和刷新按钮 | 界面 | 接29的组件结构 |
| 32 | 0a3beda | 折叠预览、移动参考设置 | 界面 | 接31，保留用户草稿与组件状态 |
| 33 | 887f1da | 视频批量操作浮动布局 | 界面 | 接32工作区，不扩大批量权限或自动提交 |
| 34 | 4e86bac | HC V3多模态图片/动作/声音 | 适配 | 依赖22/23及06协议能力，保留P4出站规则和冻结版本 |
| 35 | 77b99d7 | HC声音在预览/单次/批次统一编译 | 适配 | 必须跟随34，不只修一个入口 |
| 36 | 46752db | 移除冗余音色提示 | 界面 | 依赖已有声音参考组件 |
| 37 | aa4e955 | 移除重复声音状态/播放提示 | 界面 | 接36组件 |
| 38 | dcee553 | HC声音使用签名URL | 适配 | 接34/35；复用协作签名能力与撤权检查，不引入无认证媒体出口 |
| 39 | 00f2640 | 上传和生成音色统一 | 适配 | 接23/26/38，新增上传服务；资产归属/版本/候选/上传校验需贯通 |
| 40 | b5c4864 | 参数恢复后重新判断视频新鲜度 | 适配 | 签名/指纹必须匹配显式采纳结果；禁止直接清除旧候选的stale |
| 41 | 12b6608 | 已有原著引导追加章节 | 适配 | 涉及app而非纯文案；保留章节授权/版本与重复导入保护 |
| 42 | 11bc482 | 活跃章节任务时禁用重复提取 | 适配 | 接21/41，前端提示不代替服务端原子准入与幂等 |
| 43 | 7451368 | 集审核按钮靠近保存 | 适配 | 仍保留协作review.submit/approve权限及revision；不随55移除审核 |
| 44 | 1cb7a94 | 改编到剧本保留当前集 | 适配 | 页面选集与本地草稿/晚回执作用域检查，不将选集变共享写入 |
| 45 | a3b9e6b | 分集切换渐隐反馈 | 界面 | 接44路由；不能用遮罩掩盖旧请求覆盖新集 |
| 46 | fcbbe4c | 加深切集遮罩 | 界面 | 依赖45 |
| 47 | 8fe968d | 紧凑任务中心筛选 | 界面 | 保留作品级权限过滤和当前任务作用域 |
| 48 | b5ece03 | 分镜图批量按钮浮动 | 界面 | 保留批量提交对象权限与可提交项语义 |
| 49 | b2881e0 | 编辑素材默认当前集 | 适配 | 依赖05/07的素材助手；仅UI默认过滤，合法跨集引用不能被删除/失联 |
| 50 | 7afcf70 | 折叠分镜图编辑详情 | 界面 | 保留隐藏后的草稿、负责人和保存/冲突反馈 |
| 51 | 4f9faaa | 生成中仍可看提交预览 | 界面 | 依赖22组件；预览只读无生成副作用，改参数仍受对象权限保护 |
| 52 | 247d314 | 长镜头描述换行 | 界面 | 独立CSS候选；合入时确认选择器及布局仍匹配 |
| 53 | f4e3b53 | 视频批量复核视口弹窗 | 界面 | 依赖当前VideoProductionWorkspace；保留服务端复核和焦点/关闭行为 |
| 54 | 81ec32e | HC素材组跨实例重名 | 小修 | 仅移命名修复；SQLite测试改真实PG独立数据库隔离，建议第一批 |
| 55 | 4b474aa | 单人策划/剧本免审核 | 不原样导入 | 去掉review/approve且draft可继续，与协作审核契约冲突；其他有价值hunk只能另审 |
| 56 | 9b55ccf | 统一分镜图参数和提交规格 | 适配 | 接01；不复制单机模型池/本地启动能力，接平台参数规则、对象revision与任务冻结 |
| 57 | 13939a8 | 直接写剧本和统一分集入口 | 适配 | 建立在55后的上游结构，但不引入55免审核语义；assist应接P5候选和P6幂等，正式正文仍单主源 |

## 3. 已确认的首批候选与排除依据

### UPSTREAM-SYNC-01（建议）：HC素材组唯一命名

完整源提交：`81ec32e59150e2d12092a38ddaeced07387f7fbe`。已读该提交完整差异及协作版 `_asset_group` 对应实现。

当前协作版仍在缓存未命中时发送固定组名，因此同一HC账号的独立数据库实例仍有同名冲突风险。本轮未调用真实HC账号复现，不把代码判断当真实供应商验证。

最小移植：缓存未命中时组名追加UUID的12位十六进制后缀；保留现有PostgreSQL语法、数据库封装、账号哈希、缓存键、进程锁、已配置组ID优先及原HTTP检查。不得修改/删除云端旧组，不新增查询接口，不依赖06/34的新增协议功能。

验证建议：在任务自建隔离PG中模拟两套独立数据库共用同一合成账号，使用MockTransport模拟云端账号级组名唯一性；验证两组不同且切回原库复用，已有缓存零请求、显式组ID优先、名称长度合法且不含合成Key、原HTTP错误仍传播。复用项目PG夹具安全规则，不使用历史库，不复制上游sqlite3夹具。是否需要双数据库最小帮助函数以实际现有测试设施为准，不新建测试平台。

### UPSTREAM-SYNC-02（候选）：API错误响应只消费一次

完整源提交：`83055bb5d7d0d5560782ca1aed17ea2995e22fe6`。已读完整差异；协作版 `src/main.tsx` 仍先 `r.json()`，失败再 `r.text()`，存在响应流已经消费的错误路径。

移植纯解析函数和测试，并仅替换错误解析片段；原CSRF、认证、status/url及协作409处理保持不变。测试JSON错误、文本错误、空响应，补现有协作客户端专项及build。不要借此重构所有请求层。

### 明确不照搬的语义

- `8e17006`：源码可见409路径调用 `syncLatestProject`，清除dirty及session草稿并覆盖页面文档；不能替代P5冲突机制。
- `4b474aa`：上游 `docs/single-user-workflow.md` 明确删除审核UI并允许draft/review/approved都作为可用保存内容；不能替代协作审核。
- `dac84be` / `0cff9aa`：上游保留local及原始providerId/modelId，并按厂商/模型前缀选默认。协作版 `backend/generation_policy.py` 只接受已发布平台model_id、无默认明确失败。这是不同契约，不宣称两个实现完全等价。
- `504e0c4`：单机运行设置不恢复；协作平台配置集中在受权后台。
- `24f4c52` 中的启动模型迁移不得随协议修复带入。上游对旧数据的处理不是协作库迁移授权。

## 4. 批次顺序与交付方式

1. 审核本清单，确认首个单一任务为54；然后16另批处理。
2. 编辑器/初剪一条链：05的必要素材字段 → 07/09/12/17；11和10按可独立测试边界拆开。Twick补丁需验证当前安装版本、重复运行与未知构建失败行为；上游只新增postinstall，未改依赖版本，不等于补丁天然兼容。
3. 图像参数/视频过期、章节/分集交互等分别派发小任务；纯界面改动可依其前置业务就绪后穿插，不为凑提交数合入无用页面。
4. 多模态/音色按22 → 23/26 → 34/35/38 → 39的业务依赖推进，带上对应界面和测试，不能“界面可点但提交未实现”。24/25资产历史单独审核。
5. 直接剧本57最后独立明确协作入口、正式正文、候选及审核标准；不能用55作为免审核前置。

这不是要求一次开发所有列出的新能力；新接口/数据语义由指定ChatGPT审核会话分批明确，仍以功能实现为中心，不建设同步平台、共享库或运维体系。每批完成和外部通过后再进入下一批；新出现的上游提交不悄悄扩大本次冻结范围。

建议为同步任务采用临时 `codex/upstream-sync-20260917` 集成分支/独立worktree，以冻结验收基线；这是对master直接开发约定的任务级例外，提请ChatGPT确认后再创建。最终是否及何时回到master明确记录，不强推、不重写历史、不用reset/revert/clean处理用户改动。

独立补丁可用 `cherry-pick -x`；混合提交优先手工最小适配并在提交信息记录完整Upstream-Commit。Git无冲突不代替业务验证。不整份覆盖 `app.py`、`main.tsx`、`worker.py`，不复制数据库、媒体、Key或本机配置，不向upstream推送。

每批报告只记来源SHA、目标业务SHA、已运行测试及未验范围、外部结论和下一单一任务。无需大量重复文档。业务变更以相应真实PG、fake协议、前端测试/build和必要浏览器链路验证；旧阶段证据继续注明其旧SHA，不伪装为当前重跑。

## 5. 本轮交付与停点

仅本清单，无产品代码修改。清单覆盖数/唯一SHA/与冻结git log一致性及 `git diff --check` 是本轮应执行检查；产品测试、构建和真实供应商调用未运行，也无此轮文档任务的执行需要。

提交推送后交指定ChatGPT会话审核，说明唯一审核目标是 `imherro/OurVideoCreator`，请求下一单一任务及分支例外决定。Codex不自行签发PASS，不把历史“功能收尾结束”解释为禁止用户新授权的选择性同步，也不借同步恢复任何延期平台。
