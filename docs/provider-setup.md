# 四服务快捷配置

2026-09-18。本轮只简化平台配置入口，五角色生产流程尚未编码。自测交付状态：READY_FOR_REVIEW；不代表外部验收或真实供应商鉴权通过。

平台管理员打开 `/admin`，在四张卡片分别填写自己的 Key 并保存。保存只写入加密凭证和模型目录，不调用供应商生成接口。火山方舟 ARK Key 与豆包 Speech Key 分开填写。页面不会显示已保存 Key；更新时留空保留。

## 单人版默认值

来源：只读 MyVideoCreator `564dd223873192ec9d7831ffdbbd5c7f1a6b9d84` 的公开代码默认值；不是实时查询的供应商目录，也不包含单人版账号数据。

| 服务 | 文本 | 图片 | 视频 / 语音 |
| --- | --- | --- | --- |
| RunningHub | `bytedance/doubao-seed-2.1-pro` | `seedream-v5-pro` | `bytedance/seedance-2.5-token` |
| 幻场 AI | — | — | 默认 `doubao-seedance-2.5`，可选 `doubao-seedance-2.0` |
| 火山引擎 | `doubao-seed-2-1-pro-260628` | `doubao-seedream-5-0-pro-260628` | 默认 `doubao-seedance-2-5-260628`，可选 `doubao-seedance-2-0-260128` |
| 豆包语音 | — | — | `seed-tts-2.0`，默认 Vivi `zh_female_vv_uranus_bigtts`，MP3 / 24 kHz |

RunningHub 一个 Key 原子写入媒体与文本两个显式服务地址，不在运行时隐式跨域。幻场的参考素材公网地址可暂时留空；使用本地参考素材前必须另行具备其可访问地址，本轮不做公网部署。

第一次保存卡片默认勾选“同时设为对应用途的默认模型”：只设置该卡片的主推模型，不把可选的 2.0 设为默认。多个服务提供同用途时，以最后一次勾选保存为默认；不改变已有作品的模型选择。取消勾选仍补齐缺失模型供选择。已有自定义模型参数不会重置；复杂调整在高级配置中完成。

## 保留与安全边界

- 仅平台管理员可读写快捷配置 API，普通作品角色不获得权限。
- 复用原版本、凭证加密、审计和模型校验机制，无新增依赖、数据库迁移或付费调用。
- 每张卡片的服务、凭证及模型更改整体提交或回滚；旧页面保存返回冲突，输入草稿保留。
- 相同地址/模型有多个候选，或快捷标识已被自定义时，提示到高级配置核对，不猜测覆盖。
- 2026-09-18 用户授权修复本机旧验收库：完整数据库备份后改用原已持久保存的本机加密密钥，停用两个旧模拟供应商。46 张非目标表逐表内容摘要前后相同，作品、素材、任务和旧凭据全部保留。旧测试密文不会被新密钥解密或自动替换；历史记录不据此恢复可调用状态。该操作仅针对本机明确授权的测试配置，不是产品启动时的自动修复逻辑。

## 实际验证

- 真实隔离 PostgreSQL：`tests/test_provider_presets.py`、`tests/test_p4_model_foundation.py`、`tests/test_p4_r1_regressions.py`，63 passed。
- `npm test`：288 passed；`npm run build`：通过（保留现有大 chunk 提示）。
- 浏览器在本机 7878 显示四张卡片、可选模型和折叠高级入口，加密服务警告已消失。
- 修正旧 P4 参数回归测试夹具：对照模型发布后新建测试作品，以符合已存在的“新作品冻结全部启用模型池”规则；不改变业务权限逻辑。
- 未运行全量后端测试；未验证真实 Key 鉴权、实际模型账号可用性、供应商生成或公网参考素材读取。所有 Key 保存和加密测试使用合成值，真实 Key 留给用户填写。
