# P6-SINGLE-01：恢复单 Worker 主链与提交幂等

> 来源：指定项目 ChatGPT 会话最后回复的同名附件，2026-09-17 已打开预览并完整读取。以下为附件正文的 Markdown 转录；下载按钮已点击，但未取得可核实的本地原始下载文件，不声称字节级下载归档。

## 1. 本次授权及旧任务替代关系

用户已确认缩减方案。本文件是新的执行授权，替代此前 P6-JOBS-01 / 旧 P6_CODEX_PROMPT.md 的本轮实施要求。只操作协作仓库 imherro/OurVideoCreator，不要使用原单机版仓库。唯一任务：

**在保留全部已有工作的前提下，恢复可运行的单 Worker 任务主链，完成作用域提交幂等，并保留能够独立成立的 FFmpeg 资源隔离。**

- P0–P5 的历史通过结论不变。
- 已知已提交基线：afd1eb0d3eed36b3dcd818ed0c29aec2989c2259。
- 用户交接中的 P6 代码均为本地未提交工作，不能用远端 HEAD 代替检查这些文件。
- 本次允许相关代码编辑、必要迁移调整、测试和固定业务提交；验证完成后允许向同一协作仓库提交并推送业务及脱敏交付材料，不自动合并其他分支、不部署。
- 本次结束后停在 READY_FOR_REVIEW 或如实 BLOCKED，不得自行签署外部通过或继续新增下一项任务。
- 多 Worker、完整配额、自动恢复平台、P7、公网部署及真实付费 Provider 调用均不在本次授权内。

旧总册中的双 Worker、全量 JOB/QUOTA 矩阵，凡超出本文件的项目，明确登记为延期，不再作为本次必须完成的门槛。保留旧文档和历史验收原件，在当前执行入口注明“旧 P6 范围已被本文件替代”，不要抹去历史，也不要将延期项标成通过。

## 2. 接续当前工作，而不是从头重做

先读适用的 AGENTS.md、本文件和用户最近的暂停交接；只读核对实际分支、HEAD、origin、工作区差异。已知当前涉及：

- 已跟踪：backend/app.py、database.py、provider_egress.py、providers/common.py、providers/hc_atom.py、store.py、worker.py；阶段 README/GATE_LOG。
- 新增：backend/job_admission.py、job_execution.py、job_quota.py；0007_job_attempts.py；P6 admission/execution/quota/worker 测试及相关文档。

若实际状态已有变化，先说明差异；不要强制 checkout 旧 SHA、覆盖其他工作或按旧清单批量回滚。在整理前，先在仓库外的专用本地目录保存可恢复快照：当前变更清单、tracked diff 和本轮新增的代码/迁移/测试/文档文件。原测试日志本地保留。不复制用户数据库、媒体、Cookie、密钥等资源，不把未脱敏日志推送到 GitHub。

保留成果不等于全部接入运行时。允许针对未发布的 P6 代码做有记录的撤接和整理；未启用实现与专用测试可保留在清楚标识的延期区或本地快照。不得使用 reset --hard、git clean 或整目录覆盖代替逐项整理。不另开架构研究阶段。先用一段简短说明确定保留/暂存内容及必要依赖，然后在本任务内实施最小方案。

## 3. 本次保留范围

### 必须完成

**A. 正常任务主链恢复。**

获权用户通过现有提交入口得到 job ID；现有单 Worker 能执行任务并归档候选/素材。不得因未完成的配额或未发放的 execution token 阻断提交与执行。交接指出 create_job_record 无条件调用 quota.reserve，而日期 SQL 报错。目标不是“只修 day 语法然后继续把旧 P6 做完”，而是解除未启用功能对主链的依赖。若本次不启用配额，应撤下活动路径中的预留/释放等调用及必要耦合；不要用永远返回成功的假 reserve 或吞掉异常来假装额度有效。

**B. 六类已修改提交入口的作用域幂等。**

复用现有 job_admission、submission_id 和数据库约束，按实际代码列出六类入口，不为凑数量新增接口。相同作用域、相同键、相同规范化请求返回原任务或原批次；相同作用域和键但不同有效输入返回409。作用域应包含用户、团队和入口等必要身份；项目/目标及关键参数纳入请求区分。不同用户或团队不能因原始键相同命中他人的任务。回放前复核当前访问权限，不能因掌握幂等键而绕过撤权；保持已有对象负责人、assignment epoch、模型规则和批量原子校验。幂等判断和任务/批次创建必须在数据库中一致完成，有限并发不能各自创建一份。不能用生产 Python 锁或内存 Map 代替数据库正确性。回放不重新绑定最新模型/Key、不重新发送生成，也不因回放本身追加任务或素材。

**C. 保持已通过的运行边界。**

仍只允许一个 Worker 进程消费同一任务库，保留已通过的数据库排他与失锁处理。第二 Worker 明确拒绝是本轮允许且需要保留的行为；不要只删旧排他锁，也不要让未接入的 lease/token 原语冒充多 Worker 支持。保留 Web/Worker 独立、平台统一模型/凭证版本、受控出站、P3 权限、P5 对象保存及候选明确采纳。不要为了恢复队列而绕过这些既有检查。

**D. FFmpeg 自身资源隔离。**

优先保留已经写出的独立临时目录、精确记录自身子进程和收尾逻辑。成功、失败或取消只处理本任务创建的文件/子进程，不误删已登记输出，不清理其他任务或用户进程。不要求本轮实现独立资源调度平台。

### 按最小依赖决定，不强求随本次接通

句柄保留及取消竞争保护在单 Worker 下有价值：如果少量现有代码/字段即可独立成立，就保留并验证；如果按当前实现必须连带接通整套 attempt/checkpoint/lease，则先暂存相关新增实现，保留 P0–P5 已通过的行为，在报告说明。此选择不需要为每个文件反复申请确认，但不得无声扩大任务。未知提交状态允许保守停止并说明需核查，不强求所有供应商自动恢复；不得新增对可能已受理请求的盲目重发。已有 provider_job_id 的路径继续使用原 handle、原配置/凭证，不因本次整理退回重新生成。

## 4. 明确暂缓的内容与迁移处理

本次不继续接通多 Worker claim/heartbeat/接管、分布式 fencing、逐步骤 checkpoint 平台、全套输出去重恢复、日额度预占、分层并发、公平调度、队列年龄、远端槽位、人工对账或新管理页面。当前已有实现保留待用，但不声称这些能力已生效。

0007 仅在自有隔离测试库运行、尚未发布，可按保留功能重整，或暂时保留不影响主链的未启用字段/表。不要求为表数量而大拆迁移。要求是：

- 全新隔离库能按正常 Alembic 流程升级；正常 Web/Worker 启动不自动迁移、删库或导入旧数据。
- 本次启用代码与结构一致；延期的模块/字段不会变成正常请求必须满足的前置条件。
- 尚未启用的表、配置和能力在简明报告中列清楚；不能因存在表就宣称调度或计量已完成。
- 仅为旧 P6 扩展目标编写的测试可以保留在明确延期区，不计为本次通过，也不伪装成正常跳过。当前范围内的失败和原 P0–P5 回归不能移走以换取绿灯。

不得触碰用户或历史服务数据库。既有隔离旧库无需迁移或清理，用本任务新建测试库验证即可。

## 5. 最小验收测试

优先复用已有夹具和测试。已有失败日志如实保留，不要求每个改动都另造红测；有状态竞争的关键点用可控等待/释放或数据库轨迹，不用扩大压力规模代替确定性验证。

| 组别 | 通过要求 |
| --- | --- |
| 正常主链 | 新隔离真实 PostgreSQL 中，普通获权用户通过实际应用入口提交；实际单 Worker 完成一条文本任务和一种已支持异步媒体任务。使用现有自有 loopback fake，检查提交/查询/下载账本、任务终态和候选/素材；不自动覆盖人工对象。入站可以用现有 TestClient，不把它写成浏览器或真实入站网络。 |
| 幂等并发 | 有限多个请求使用同一作用域、同一键和输入，只建一个任务/批次；在所选无故障 fake 流程中生成提交不重复。相同键不同输入409。这里验证提交幂等，不宣称端到端 exactly-once。 |
| 权限与批量 | 不同用户/团队同原始键隔离；撤权后回放拒绝；混入非法对象/模型参数的批次不半写。六类实际已改入口至少各有正常及适用拒绝/回放路径；共用原语的所有交错不必机械复制六遍。 |
| 单 Worker 边界 | 原数据库排他、第二 Worker 拒绝及 Web 重启不牵连 Worker 的既有回归保持。不得为了通过本次测试启用多个消费者。 |
| FFmpeg | 一次短片成功导出，加一次失败或取消收尾；只清理自身临时资源，另一个任务的测试哨兵文件/进程不受影响；输出仍可读取。只操作测试自身资源。 |
| 句柄/取消（仅当随本次保留新增实现） | 已保存的远端 ID 不被迟到状态清空；恢复/查询不重新生成；取消与完成规则明确且用已有 fake 验证。不能把没有执行的路径写成通过。 |

由于本次实际改动 app/store/Worker/出站，在最终固定业务版本运行一次完整后端回归，并保留关键原始输出和失败记录。不要每个小修改都机械跑全量，不能累计不同开发批次的通过数冒充最终结果。前端、依赖、界面和相关契约未变时，可以继承 P5-R2 对应证据，不要求重跑整套前端/浏览器。若确实改变用户可见任务状态或操作，则只补受影响页面/前端验证。不得为了免责而无故新增 UI 需求。

## 6. 交付和停止

默认只需一份简明 REPORT.md，配固定版本的关键测试输出和最终后端回归日志。不强制再制造多份重复 MATRIX、SUMMARY、PROVENANCE 或大量截图。建议新证据目录：docs/multiuser-rollout/evidence/P6-SINGLE-01/。报告写清：

- 实际开发 base、最终被测业务 SHA、随后仅文档的 evidence HEAD。
- 保留/启用的功能；暂存未启用内容、必要迁移决定，以及没有执行的验证。
- 真实命令、退出码、测试结果、关键幂等竞争及 fake 计数；开发过程中的失败/中断与固定版本结果区分。
- 现有单 Worker 边界、配额未启用、没有真实付费联调/公网部署。不要将本任务通过预写为整个旧 P6 平台通过。

阶段 README/GATE_LOG 只追加本次范围调整和待审状态，历史通过/失败记录不改写。可沿用旧日志作为历史来源，不将其重新标成新 SHA 的运行。仅使用本次明确创建的新测试库、端口、进程及目录；保护所有用户和历史测试实例，尤其已交接的7868、7895、6313、6185、7028及P5保留环境。清理只限测试自身、身份确认无误的资源；不按模糊进程名或目录通配符批量 kill/drop。公开证据不得包含密码、Key、Cookie、完整DSN或无关用户内容。完成后提交到协作仓库，提供可审查的完整 SHA 和报告路径，状态只写 READY_FOR_REVIEW；遇到真正的环境或现有代码依赖阻塞，提供具体证据并停在 BLOCKED，不要顺势扩成原 P6 全部功能。

本授权是实现一个小而完整的单 Worker 任务闭环，不是完成旧 P6 任务平台。交付后停止，等待外部验收和下一次范围决定。
