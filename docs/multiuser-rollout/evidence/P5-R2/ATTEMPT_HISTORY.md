# Attempts retained without overwriting results

1. Runtime 78fd14f; initial new fixture used `const copy=structuredClone` with Array.map, which incorrectly passed the index as clone options. 8 failed / 0 passed; this is **fixture failure, not product red**. UTC 2026-09-16T23:18:57.5121122Z–23:18:58.4815337Z, exit 1. Retained `baseline/baseline-host-red.txt`.
2. Fixed fixture only (`value=>structuredClone(value)`); runtime unchanged. 5 failed / 3 passed, exit 1, UTC 23:19:12.2907228Z–23:19:12.9583453Z. Retained `baseline/baseline-host-red-valid.txt`. Source raw hash cc28f13d51272be24016f344d5b6f18adf61babca089a23a3af145c181a8427e.
3. Added final-note test before runtime patch to exercise the actual overwrite end-to-end in the mock. 6 failed / 3 passed, exit 1. Frozen test and raw output in baseline; binding/UTC in REPORT.
4. Initial ObjectDrafts/CollaborationClient repair made all 9 initial cases pass. Added scope/epoch/Y cases: 13 passed / 1 failed (global old-scope error changed new page status). `development/scope-developer.txt`, exit 1. Fixed host guards, expanded to both same-ID and new-ID scopes; 16 passed, exit 0, `development/developer-green.txt`.
5. First full suite after state-machine repair: 194 passed / 1 failed, exit 1, `development/developer-frontend.txt`. Shared chapter test expected automatic remote adoption. Updated it to assert the authorized conflict behavior, compare retention, blocked retry and explicit discard: 195 passed, exit 0, `development/developer-frontend-green.txt`. Development build passed; large-chunk warning retained. Development logs are working-tree runs, **not** relabeled as fixed-SHA evidence; timestamps were not separately captured for steps 4–5.
6. Committed business aebffe3, then ran all formal checks at that exact SHA. `commands-aebffe3/runs.json` contains recorded UTC/commands/exits. Formal evidence supersedes working-tree runs for delivery binding, but does not erase failures.

Public logs redact only local profile/repository path prefixes. Semantic lines, failure counts and trailing whitespace in raw Node output remain intact. SHA256SUMS uses LF-normalized Git content. No secrets or test account credentials were used by these frontend-only tests.
