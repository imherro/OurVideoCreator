# P5 正式取证尝试历史

## f4773f0 第一次固定业务提交

- 基线：`c0152ffcfb5623fcc0b723dbdf450acec9f17f73`，P4-R1 外部通过。
- 被测业务：`f4773f0075325625c6cf2e6236c5d2014d416d3f`。
- 正式命令开始 UTC：2026-09-16T21:40:24.840136+00:00。
- 开发期完整后端 458 passed / 2 warnings 不替代此次固定提交的最终日志。
- 原始 P4-R1 审核附件保持字节不变，其中 MATRIX.md 原有 Markdown 两个行尾空格被 staged diff --check 报告；排除原始审核附件后的修改检查通过，没有改写审核原件。

真实浏览器使用两个独立 loopback cookie host、两个普通 editor 账号和第三个同用户旧页面。测试账号无 platform_admin/owner 权限，角色和资源均由公开接口建立。仅本机受控 fake，无真实供应商调用。原有用户/测试服务保留。

观察到的成功路径：

- A/B 各自修改镜头 X/Y，实际保存、刷新、退出/重登录后两项均保留。
- 第二 A 页面暂时离线留下 r2 草稿；主 A 保存 r3，旧页恢复联网明确保存得到真实 commands 409。比较同时显示本地旧草稿和远端新内容，保存/审核禁用；观察窗口只有读取，没有自动重发 commands。已恢复网络，冲突草稿仍保留。
- 同用户两页时间线 acquire 竞争，第二页 lease_busy；第一页面 renew/release 后第二页面 acquire 成功并 release。不是仅靠禁用按钮作证明。
- 第一任务 fake 收到 v1 后阻塞，人工保存 v2；放行结果成功只为候选。其间为解决重叠测试节点点击自动排列改变 graph revision，明确采纳正确拒绝该过期引用；保留此拒绝，不冒称首任务已采纳。
- 第二任务由实际开始生成按钮提交，fake 收到后人工保存 v3；放行只候选。B 可以比较但采纳禁用，A 明确采纳后 r4，B 看到采纳收据。两个 fake 请求各有独立原件。
- A/B 不同选中节点和缩放，在共享正文 v5 同步后保持不变。
- 实际导演台新增角色并保存 PNG，成功转到新图像节点并带真实参考图；B 全对象已保存，没有假 graph 冲突。预览截图可见角色与地面且无网格。

发现的未通过项：

- COLLAB-12 扩展到实际剪辑页面时，A 持有租约新增标题轨并保存 timeline r3，B 对象协作面板已收到 r3，但已打开的 Twick 编辑区仍是空时间线。
- 源码确认 EditorWorkspace 的 initialTimeline 仅按 projectId 初始化，缺少后续远端输入应用。这是真实前端同步缺口，需要修复并在新 SHA 重跑受影响的前端及浏览器验证。
- “Jump to end”立即回到 0 的现象不能当作播放头独立证明；组件在播放到 duration 时本就回到 0。需要改用时间尺中间位置验证。DOM 的“正在同步预览”来自 CSS 伪元素，实际截图已显示正常标题预览，不能仅据该文本判定播放器阻塞。

本尝试不声明 P5 通过，不进入 P6。完整命令现已全部结束：458 后端通过、10 并发轨迹通过、172 前端通过，TS/build/routes/compile/diff 全部 exit 0。原件见 commands-f4773f0。

## 开发期修正（不冒充固定 SHA 正式证据）

- 补上 host document → 已打开 Twick 的输入同步；区分远端载入、本地变更和保存回执，不能回声保存。
- 播放头回零的具体原因是标题轨 type=text 不属于当前 Twick scene renderer 支持的轨类型；改为 element，标题元素仍为 text。保留内容、时间和媒体引用，不修改 node_modules。
- 规范化旧标题轨时发现首次挂载被误认为编辑，B 被客户端负责人校验拒绝、草稿保留；修正初始化 baseline。开发期失败不当作最终 SHA 的结果。
- 浏览器缓存一度仍加载前一构建，随后禁用测试 tab cache 并检查 script URL。开发期误点了预览测量 DOM 的同名标题；改用真实 .twick-track-element-content。
- 一次 npm test 176/177 通过，legacy 断言仍期望不可播放的 text 轨；保持内容/其他音轨/空隙断言，仅明确预期规范化为 element，补充幂等和输入不变测试。最终 178/178。

## 0851dce 固定修正业务提交

- `0851dce02e0f22e87f6247b029fd3faedd39e7dd` 只改 4 个 editor 源文件、3 个前端测试文件。
- 后端、迁移、scripts、依赖与 f4773f0 完全一致，由 capture_followup.py 实际 git diff 检查；保留完整后端原证据，正式重跑前端 178、TypeScript/build、真实 editor renderer 30 测试、diff-check，均 exit 0。
- 正式补跑 UTC 22:13:59—22:14:31；浏览器窗口 22:13:38—22:21:00（结束上界，不代表持续执行到该时刻）。bundle index-DuioqIPf.js，含最终核对共17次调用/17次返回。
- A 0.5 秒、B 1.5 秒；A 选中标题，B 无选择。A 保存后 B 未 reload 即收到新标题，播放头与选择各自保持。
- B 未授权编辑被客户端拒绝保存且草稿保留；A 保存新正文到 r9，B 比较显示本地草稿与远端 r9。明确放弃测试草稿后载入远端，仍停在 1.5 秒。不能冒称 B 收到网络 403；服务端拒绝由 API 测试覆盖。
- A 实际开始导出，独立 Worker 使用本机 FFmpeg 完成 3 秒 1280×720 / 24fps MP4（asset-6e49a6f665f045c3828a7d9dd7ebb3d4）。未调用真实 Provider。
- A 租约明确释放；第二 A 旧镜头冲突草稿仍保留。状态 READY_FOR_REVIEW，不自签 PASS。
