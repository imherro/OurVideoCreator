# P5-R1 实际尝试记录

1. 主会话 P5 首轮外部审核完成：不通过，唯一正式阻塞 OVC-P5-01。已下载完整 ZIP 并逐字读取 REVIEW、MATRIX、完整 P5-R1 提示词和审核回归草案；原件不改写。首次点击下载未立即找到文件，使用浏览器 download 事件重试后在下载目录找到包；不从隐藏网页接口取文件。
2. 初次执行审核草案因新 shell 缺少 OVC_TEST_ADMIN_URL 而 conftest 退出4（UTC 22:46:23—22:46:25）；`environment-attempt.txt`保留异常，不计产品红测。随后从本任务先前已授权的测试启动配置恢复到仅当前子进程环境，没有输出DSN或更改系统环境。
3. UTC 22:47:18.2459083Z—22:47:36.2681580Z，在5143ee3实际运行审核提供的3个PG/API用例，3 failed/exit1；三入口均200且拓扑错误，原始输出见baseline-red.txt。
4. 公共commands最小修复，新增12个结构相关用例，合并审核草案共15 passed/63.71s。真实PG等待两请求包含blocker链，结果[200,409]。未mock权限、commands、事务或验证函数。
5. 读取开发测试进度期间一个只读pg_stat_activity诊断语句因interval字符串引号错误退出1，没有修改数据库；随后原专项正常结束，通过63.71s，不把诊断失败算产品失败或“已发生死锁”。
6. 前端只新增命令级回归，无运行时代码变化；20个适配器测试通过、完整npm默认套件179通过。已有结构编辑确实同时提交graph当前版本。额外迟到回执红测单独移至deferred目录，保留原日志并明确未修复；已发外部主会话评估范围，未静默删掉失败证据。
7. 固定业务34d646d后，于UTC22:51:58.9606649Z启动正式专项，随后脚本执行完整后端及其余验证；当时尚待完整结果、本地业务未推送。
8. 正式专项15 passed/66.21s；capture在UTC23:13:49.061445Z完整结束exit0，473后端/10并发/179默认前端，tsc/build/routes/compile/diff全exit0。完整日志原件复制到commands-34d646d，不修改失败历史、warnings或早期输出。原session20970结束后未重启。后续独立docs提交与外部复验使用这些已完成结果。
9. 主会话补充正式确认OVC-P5-02（S2），授权R1独立交付后执行R2。完整原件和提示词已下载校验，R1没有混入前端运行时修正，不把默认179通过包装成全部P5缺陷解决。
10. 最终staged diff检查发现deferred/frontend-late-ack-red.txt的Node原始失败格式在22/25行带空格。保留原始日志，不为换绿删改输出；排除该唯一原始日志后的staged diff检查另行通过。正式业务capture的diff-check仍是原真实结果，不改写为后来所有新证据的检查。

所有P5和前阶段原始材料保留；不清理用户服务或历史测试实例。新日志公开前仅脱敏本机用户路径并检查已知凭据模式，原始本地日志不覆盖；不宣称全量秘密形式化证明。
