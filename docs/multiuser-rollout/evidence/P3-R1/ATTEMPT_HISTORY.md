# P3-R1 尝试记录

本目录的正式日志全部绑定业务 SHA `a8cd59e8247b915737084383f5b53df1f831f4fe`，没有用补跑覆盖失败历史。

业务提交前首次运行 P3-R1 定向测试时，手工注入的孤立 `production_members` 行仍能通过 `GET /api/projects` 列表泄露作品。该次失败发生在业务提交前；修复为列表查询同时要求有效 `workspace_members` 后，增加同一断言并重新运行定向测试，得到 12/12 通过。这个失败是 `OVC-P3-02` 读端纵深防御的一部分，未被隐去。

随后在业务 SHA 上正式运行：P3-R1 定向 12/12、Python 全量 288/288、前端 126/126、构建、路由守卫和编译检查均一次通过。浏览器闭环使用独立端口、数据库和两套 Cookie 上下文；服务与数据库在取证后清理，未触碰 7868。
