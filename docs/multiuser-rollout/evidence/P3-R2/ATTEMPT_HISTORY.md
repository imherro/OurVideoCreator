# P3-R2 尝试记录

1. 初版同步修复提交 `4a77a39`。补查发现重置消费在等待前读取时间，00 的到期探针得到 200 而非 400（exit 1）；最终业务 `cd61996` 将时间读取移到锁后。
2. 初次真实 loopback HTTP harness 受进程代理环境影响，3 failed / 1 passed（01，exit 1），请求未到验证屏障；一次诊断观察到 502。测试客户端改 `trust_env=False`，仅禁止测试继承代理，不改业务网络或系统设置。诊断没有独立完整原始日志，不能当作正式证据。
3. 基线 worktree `5e0c5e3` 复制新探针，02 空桶 budget 红测：11×401、0×429、verify 11（exit 1）。此前探索性运行未单独存盘，不倒填。
4. 同基线 03：真实 PostgreSQL 三请求死锁和并发签发留下双有效 token（2 failed，exit 1）。正式修复测试并非只让请求同时起跑，包含验证/revoke 关键点及 PG 等待观察。
5. 固定 `cd61996` 后正式跑 04–09：定向 27、全量 303、前端 126、build、route、compile 通过。保留既有 warnings。
6. 真实浏览器 10/11：Chrome/Edge 扩展不可用，使用 IAB 两个 hostname 的独立 Cookie 上下文；不称双 profile。CDP 历史在导航/人工控制后有缓冲截断，保留警告，使用仍可核对的当前响应及实际服务器访问日志补充。
7. 两次早期断言不匹配实际刷新行为：刷新回首个团队，而不是保持上次团队；切换团队请求尚未完成时立即断言空状态。记录失败及随后的实际状态，不因测试预期错误改业务。
8. 初次 owner 移除 confirm 被用户取消，没有 DELETE；改为 UI 降级最后 owner，服务返回 409，页面正确展示错误。一次重复只为取证（两次 409），没有修改成功。
9. 原成员 Cookie SSE 返回 401；一次浏览器直接导航 API 被浏览器安全机制拒绝，不绕过，使用已有 401 与登录页证据。
10. 两个移除控件工具点击成功触发 confirm，但 getJsDialog 未暴露句柄，focus/CDP 超时；用户手动确认。服务 DELETE 200、审计及刷新结果证明完成。明确人工协助而非全自动。取证中断后旧临时标签关闭，新标签复用原 host-only sessions 继续。
11. 12 为正式浏览器结束后的只读审计原始行和成员查询，13 为真实 7895 服务访问日志。未覆盖旧 P3/P3-R1 证据，没有真实付费调用、生产数据写入或用户服务停止。
