# P2-R1 尝试记录

## 开发验证

1. 新增专项测试后，定向组合首次为 8 passed / 2 failed：一个 script 回滚夹具在成功保存 adaptation 前读取 script；一个 event 测试比较 JSONB 文本键顺序。修正夹具先建立合法 adaptation，并按解析后的 JSON 比较。没有放宽业务断言。
2. 首轮开发全量为 272 passed / 1 failed：旧 Ark 单元测试直接调用 Worker 内循环，没有取得 P2-R1 新增的数据库所有权锁，所以任务按新安全契约保持 queued。测试改为显式 acquire/release，业务实现不回退。
3. 修正后专项 6/6、组合回归 74/74、开发全量 273/273；固定业务 SHA 后正式专项 10/10、正式全量 273/273。

以上早期命令只输出到开发终端，没有重定向原始日志，因此不补造日志文件。正式审核证据只使用当前目录的 `00` 至 `06` 日志，全部绑定业务 SHA `246db511bd950e649f59600a5f6cd2ed183be71e`。
