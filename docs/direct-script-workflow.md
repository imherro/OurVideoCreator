# 协作版直接编写剧本

## 已实现：手工正文与审核

- 网页新建作品默认“直接写剧本”，进入 EP01 剧本室；可明确选择“从原著改编”。旧 API 未传 `creation_mode` 时保持 adaptation 语义，不批量修改历史项目。
- 直接编写或粘贴正文不需要原著、事件、已批准规划或文本模型。保存进入草稿，负责人提交审核，管理者批准后从剧本室进入分镜规划。保存不是批准，也不会创建 AI 任务。
- 作品列表可继续新增集；新集继承最近未删除集的制作规格和创作起点，API 可显式选择 direct/adaptation。剧本列表包含实际集与规划集的并集，无规划的实际集也能读写。
- `episode_scripts` 仍是唯一正式正文；画布是读取投影。负责人、revision、assignment_epoch、冲突比较和历史不变。切集保留未保存正文，已批准正文发生未保存编辑时禁用本页分镜入口。
- 服务端初始化直接剧本的 `metadata.adaptationLinked=false`。无关改编规划更新不使其过期；明确引用的原著章节/事件变化仍使已审核正文过期。用户若明确采纳改编候选，恢复 adaptation 来源和依赖，不自动覆盖或采纳。
- `creationMode` 只是创作入口偏好，不是权限字段；不能通过保存正文伪造来源元数据或取消审核。

## 尚待适配的上游内容

上游 `13939a82` 的整项直接剧本功能尚未全部完成。AI 辅助创作接口、冻结前集/Bible上下文、独立辅助任务的候选采纳，以及统一新增集对话框/可选原著导航继续排队。不能把当前手工路径的通过视为这些部分已完成。协作版不会引入上游单人免审核或 AI 自动回写正文。

AI 辅助准备进度：`backend/direct_scripts.py` 已提供内部只读上下文构造、稳定指纹和提示词构造。读取本集正式正文、最近三集非过期非空且未删除的正文尾部（每集最多 8000 字符）、制作规格及组合后的 Bible；视觉摘要以协作对象当前版本描述为准，不读取旧文档载体。未新增 HTTP 入口，也未接入任务冻结或候选采纳；调用者仍须实施权限、版本及事务并发校验，不能把指纹本身当作这些校验的替代。此准备模块不是 AI 功能交付完成。

## 验证入口

- `tests/test_direct_script_manual.py`：真实隔离 PostgreSQL，创建、无规划 EP02、规格继承、单一投影、负责人/审核/旧版本冲突、显式来源及改编候选转换。
- `tests/direct_script_manual.test.mjs`：网页默认值、入口偏好、当前集审核引导。
- `tests/direct_script_manual_browser.html`：用 `node tests/image_settings_browser.mjs` 启动后访问此文件，只含内存 Mock；验证真实组件交互，不是登录后的业务 E2E。

没有新增依赖、数据库迁移、后台页面或付费调用。
